---
import Base from '../../../layouts/Base.astro';
import AssemblyGuide from '../../../components/AssemblyGuide.astro';
import { t } from '../../../i18n/ui';
const tr = t('en');
---

<Base locale="en" page="assembly" title={`${tr('assembly.title')} | ${tr('site.title')}`} altPath="assembly-guide">
  <AssemblyGuide locale="en" />
</Base>
