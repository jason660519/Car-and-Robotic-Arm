---
import Base from '../../../layouts/Base.astro';
import InventoryApp from '../../../components/InventoryApp.astro';
import { t } from '../../../i18n/ui';
const tr = t('zh');
---

<Base locale="zh" page="inventory" title={`${tr('inventory.title')} | ${tr('site.title')}`} altPath="inventory">
  <InventoryApp locale="zh" />
</Base>
