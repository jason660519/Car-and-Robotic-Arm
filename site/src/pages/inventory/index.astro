---
import Base from '../../layouts/Base.astro';
import InventoryList from '../../components/InventoryList.astro';
import { t } from '../../i18n/ui';

const tr = t('en');
---

<Base locale="en" page="inventory" title={`${tr('inventory.title')} | ${tr('site.title')}`} altPath="inventory">
  <InventoryList locale="en" category="all" />
</Base>
